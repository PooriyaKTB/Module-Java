`Exercise 1.1` - Product Class =>  `Product` package
`Exercise 1.2` - Reference Behaviour =>  `Product` package
`Exercise 2.1` - Fix the direct balance access using encapsulation =>  `BankAccount` package
`Exercise 2.2` - Abstraction in Code => `FileLogger` package
`Exercise 3.1` - From Downcasting to Polymorphism => `Animal` package
`Exercise 3.2` - Overloading vs Overriding => `PaymentMethod` package
`Exercise 3.3` - Composition vs Inheritance => `PaymentMethod` package
`Exercise 4.1` - Static Instance Counter => planet
`Exercise 4.2` - Final Safety => Configuration

NOTEs:
-In 4th section of prep, I just realised that to follow the package naming conventions we define our package in all lower-case. I didn't apply it so far, but will follow it now on.
